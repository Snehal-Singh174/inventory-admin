{
  "dependencies": {
    "bcrypt": "^5.1.1",
    "express": "^4.22.2",
    "pg": "^8.22.0"
  },
  "description": "StockLedger PostgreSQL database migrations, seeds, and health check",
  "name": "stockledger-database",
  "scripts": {
    "health": "curl -f http://localhost:4010/health || exit 1",
    "migrate": "node src/migrate.js",
    "seed": "node src/seed.js",
    "start": "node src/index.js"
  },
  "type": "module",
  "version": "1.0.0"
}
