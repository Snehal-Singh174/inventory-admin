import dotenv from 'dotenv';
dotenv.config();

export const config = {
  port: parseInt(process.env.PORT || '4020', 10),
  nodeEnv: process.env.NODE_ENV || 'development',
  database: {
    url: process.env.DATABASE_URL || 'postgresql://stockledger:stockledger_pass@stockledger-postgres:5432/stockledger',
  },
  jwt: {
    secret: process.env.JWT_SECRET || 'stockledger_jwt_secret_dev_2026',
    expiresIn: process.env.JWT_EXPIRES_IN || '8h',
  },
  bcrypt: {
    rounds: parseInt(process.env.BCRYPT_ROUNDS || '12', 10),
  },
};
